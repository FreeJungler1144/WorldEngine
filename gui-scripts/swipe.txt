# Both axes of the screen transition, caught in mid flight.
#
# Open INOP slides sideways. Settings comes up from the bottom while the
# main menu leaves through the top, and Escape reverses it.
wait 0.6
move 960 433
wait 0.3
click
wait 0.06
shot setup-sideways
wait 0.7
key escape
wait 0.7
move 960 595
wait 0.3
click
wait 0.06
shot settings-upward
wait 0.7
key escape
wait 0.06
shot settings-downward
wait 0.7
quit
