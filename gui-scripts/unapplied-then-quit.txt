# Opens Settings, flips Reduced motion, does NOT press Apply, and quits.
#
# inop.gui.json must be unchanged afterwards. A pending edit never leaves
# the settings panel, so leaving cannot save it. That is what the quit
# modal now says out loud.
wait 0.5
move 960 595
wait 0.3
click
wait 0.6
move 824 375
wait 0.3
click
wait 0.5
shot reduced-motion-toggled
quit
